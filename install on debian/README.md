# OpenWRT on VPS — Install Script for Debian

Скрипт для установки OpenWRT поверх Debian на виртуальный сервер (VPS) с архитектурой x86_64.  
Полностью заменяет ОС хоста: записывает образ OpenWRT на системный диск через initramfs, минуя работающую ОС.

---

## Требования

| Параметр | Значение |
|---|---|
| Хостовая ОС | Debian 11 / 12 / 13 (x86_64) |
| Права | root |
| Архитектура | x86_64 |
| RAM | от 256 МБ |
| Диск | от 512 МБ |
| Сеть | Статический IP на VPS (рекомендуется) |

> ⚠️ **ВНИМАНИЕ**: Скрипт **необратимо** перезаписывает весь системный диск. Все данные на VPS будут уничтожены. Используйте только на выделенных серверах или свежих VPS без ценных данных.

---

## Использование

### Базовый запуск

```bash
wget -O install.sh https://raw.githubusercontent.com/Van-Hoffen/OpenWRT/main/install%20on%20debian/install_openwrt_debian.sh
chmod +x install.sh
bash install.sh
```

### С заданными параметрами (опционально)

```bash
SSH_PORT=2222 LUCI_PORT=8080 SSH_PASS=MyPassword bash install.sh
```

Если переменные не заданы — скрипт сгенерирует их автоматически:

| Переменная | Поведение по умолчанию |
|---|---|
| `SSH_PORT` | Случайный порт в диапазоне 10000–59999 |
| `LUCI_PORT` | Случайный порт в диапазоне 10000–59999 (не совпадает с SSH) |
| `SSH_PASS` | Случайный 16-символьный пароль |

---

## Что делает скрипт

1. **Определяет сеть** — IP-адрес, маску, шлюз на активном интерфейсе
2. **Авто-определяет последнюю стабильную версию OpenWRT** — парсит `downloads.openwrt.org`
3. **Скачивает образ** `openwrt-*-x86-64-generic-ext4-combined.img.gz`
4. **Монтирует rootfs образа** и патчит его:
   - Прописывает статическую WAN-конфигурацию
   - Устанавливает случайные порты SSH (dropbear) и LuCI (uhttpd)
   - Добавляет правила firewall для этих портов
   - Устанавливает хеш пароля root (SHA-512)
   - Отключает dnsmasq (не нужен на VPS без LAN)
5. **Собирает initramfs** с образом внутри
6. **Перезагружает сервер** — при старте initramfs записывает образ на диск через `dd` и снова перезагружается уже в OpenWRT

---

## Вывод перед перезагрузкой

Перед запуском скрипт покажет и продублирует итоговую таблицу доступа:

```
════════════════════════════════════════════════
  OpenWRT 25.12.4 Install Ready — SAVE THIS!
════════════════════════════════════════════════
  WAN IP    : 1.2.3.4
  SSH       : ssh root@1.2.3.4 -p 34521
  LuCI UI   : http://1.2.3.4:47103
  Password  : aB3xK9mLqP2nRs7w
════════════════════════════════════════════════
```

> 💾 **Сохраните эти данные до перезагрузки** — после неё текущий сеанс SSH будет разорван.

Перезагрузка происходит автоматически через **10 секунд** после вывода.

---

## После установки

Подключение по SSH:

```bash
ssh root@<WAN_IP> -p <SSH_PORT>
```

Открыть веб-интерфейс LuCI:

```
http://<WAN_IP>:<LUCI_PORT>
```

### Первый шаг после подключения — настройка DNS

Скрипт отключает `dnsmasq`, поэтому `/etc/resolv.conf` по умолчанию указывает на `127.0.0.1`, где никто не слушает. Это приводит к ошибкам при работе пакетного менеджера `apk`. **Сразу после входа выполните:**

```bash
uci set network.wan.peerdns='0'
uci set network.wan.dns='1.1.1.1 8.8.8.8'
uci commit network
/etc/init.d/network restart
```

После этого обновление пакетов работает штатно:

```bash
apk update
apk upgrade
```

---

## Технические детали

### Совместимость паролей

Скрипт использует `openssl passwd -6` (SHA-512) — формат совместим с busybox/shadow в OpenWRT 21.02+.

### Автоматическое определение версии

```bash
LATEST_VER=$(curl .../releases/ | grep -oP версии | sort -V | tail -n1)
```

Если парсинг не удался — используется fallback на последнюю известную версию (`25.12.4`).  
Также предусмотрен fallback на `-efi` вариант образа если стандартный недоступен.

### Механизм takeover

Образ загружается в initramfs (RAM), после чего до монтирования корневой ФС выполняется:

```bash
gzip -dcq /owrt.img.gz | dd of=/dev/sda bs=4M conv=fsync
```

Это позволяет перезаписать диск, с которого загрузилась система — без Live USB и KVM-консоли.

---

## Известные ограничения

- Работает только с **одним сетевым интерфейсом** (WAN = `eth0`)
- После установки **LAN-интерфейс** (`br-lan`) настроен на VLAN `eth0.10` — на большинстве VPS он не используется
- **HTTPS для LuCI** не настраивается автоматически — порт настроен только для HTTP
- Протестировано на провайдерах с **virtio-net** (KVM-VPS); на XEN/OpenVZ могут потребоваться правки сетевого интерфейса
- **dnsmasq отключён** — после установки требуется ручная настройка DNS (см. раздел выше)

---

## Структура репозитория

```
install on debian/
├── install_openwrt_debian.sh   # Основной скрипт установки
└── README.md                   # Этот файл
```

---

## Лицензия

MIT — используйте свободно, на свой страх и риск.
